using System;

namespace Exercise
{
    class Program
    {
        static void Main(string[] args)
        {
            int amountOfSales = int.Parse(Console.ReadLine());

            while (true)
            {
                int priceObjects = int.Parse(Console.ReadLine());
                if (priceObjects <= 100)
                {
                    Console.WriteLine("Product sold!");
                }
                else if (priceObjects >=)
                { 
                 
                }
            }
        }
    }
}
